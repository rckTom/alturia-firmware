compatible: "pyros"

description: "Device tree node representing a number of pyros"

child-binding:
  description: Pyro child node
  properties:
    gpios:
      type: phandle-array
      required: true
